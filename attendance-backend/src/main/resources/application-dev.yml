# 本地开发：覆盖数据库密码。连接失败时请检查：1) MySQL 已启动 2) 已建库 attendance 并执行 docs/attendance-db-schema.sql 与 init-admin.sql 3) 下方密码与本地 MySQL 一致
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/attendance?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: '*$lt2020'   # 改为你的 MySQL root 密码，无密码则留空
