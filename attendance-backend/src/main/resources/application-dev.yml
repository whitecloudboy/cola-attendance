# 本地开发：覆盖数据库等配置。勿提交真实密码，可使用 application-local.yml（已被 gitignore）或环境变量。
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/attendance?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: ''   # 请改为你的 MySQL 密码，或使用环境变量 SPRING_DATASOURCE_PASSWORD
